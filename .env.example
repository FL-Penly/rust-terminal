# Mobile Terminal Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ============ Server Settings ============
# Server port (default: 7682, use different port from mobile-terminal's 7681)
PORT=7682

# ============ Devbox Tunnel Settings ============
# Your dev machine SSH address (user@ip)
DEV_HOST="your_user@10.x.x.x"
# Your Mac local username
MAC_USER="your_mac_username"
# Server port on dev machine
DEV_PORT=7681
# SSH reverse tunnel port
TUNNEL_PORT=22222

# ============ Cloudflare Tunnel Settings ============
# Your Cloudflare tunnel name (created via: cloudflared tunnel create <name>)
CF_TUNNEL_NAME="my-terminal-tunnel"
# Your domain pointing to this tunnel
CF_DOMAIN="terminal.example.com"
